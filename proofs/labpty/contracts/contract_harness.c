/* DFCC entry point. goto-instrument --enforce-contract wraps each decoder
 * independently, so this harness only has to exist as the --dfcc entry;
 * it does not need to call the contracted function. */
int main(void) { return 0; }

# (Reserved for future attributes.)
#
# The `.rpg/graph.json merge=ours -diff linguist-generated` rule lived here
# while the semantic graph was a committed artifact. The graph is now local-only
# and gitignored, so the rule had nothing left to match. See ADR 0013.

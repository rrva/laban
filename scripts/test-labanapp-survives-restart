#!/usr/bin/env sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_root"

if swift test --filter LabanAppTests.testLabanAppRestartPreservesChildViaLabpty; then
  printf 'child survived\n'
else
  printf 'child died\n' >&2
  exit 1
fi

{
  "name": "glyph-effects-ink-bloom",
  "version": 1,
  "description": "Control row for the ink-bloom scenario: the scenario re-outputs the same text on the next row and compares age-0 (thin/faint) and settled (identical) pixels against this never-restamped control.",
  "initialSize": {
    "cols": 80,
    "rows": 24
  },
  "steps": [
    {
      "op": "writeBytes",
      "encoding": "utf8",
      "data": "ink bloom control text\r\n"
    },
    {
      "op": "waitFrames",
      "count": 2
    }
  ],
  "expect": {
    "containsText": ["ink bloom control text"]
  }
}
